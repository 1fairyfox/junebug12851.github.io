# Single source of truth for the version number (SemVer 2.0.0).
# See notes/reference/versioning.md. PATCH = default (fixes + ordinary changes),
# MINOR = a real milestone, MAJOR (-> 1.0.0) = Twilight's call only.
0.1.0
