# Single source of truth for the version number (SemVer 2.0.0).
# PATCH = default (fixes + ordinary changes), MINOR = a real milestone,
# MAJOR (-> 1.0.0) = the project leader's call only. See the versioning standard.
0.1.0
