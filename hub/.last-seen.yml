# ── Round-up "last seen" markers ─────────────────────────────────────────────
# Records the dev-branch commit each project was at when the most recent round-up
# pass ran, so the next pass knows where to resume. Updated by the cross-project
# round-up pass (see notes/reference/blogging-workflow.md). Git-only awareness;
# nothing here is a dependency.

pokered-save-editor-2:
  commit: 005a0cbff9022a107b76657efbb9c761df332d7f
  branch: dev
  seen: 2026-06-25
  # Covered through the 0.14.x static-analysis gate + test-type breadth push
  # (blogged: _posts/2026-06-23-pokered-save-editor-2-static-analysis-and-tests.md).
  # 2026-06-25 round-up: no new dev commits since this marker — nothing to blog.

random-ai-prompt:
  commit: 96ee2ad57c14623b1d15be30591ee5794bbda10b
  branch: dev
  seen: 2026-06-25
  # Covered through 2.6.1: project-side onboarding into the fairyfox mesh
  # (mesh-aware CLAUDE.md + a fairyfox-themed JSDoc docs site) and the
  # SFW-by-default + top-bar NSFW toggle. Folded into the 2026-06-25 day post
  # (_posts/2026-06-25-nodes-that-check-the-hub-for-updates.md). The Linux
  # visual-baseline regen around the toggle is incremental test plumbing, not a post.
