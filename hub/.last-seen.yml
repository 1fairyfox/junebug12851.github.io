# ── Round-up "last seen" markers ─────────────────────────────────────────────
# Records the dev-branch commit each project was at when the most recent round-up
# pass ran, so the next pass knows where to resume. Updated by the cross-project
# round-up pass (see notes/reference/blogging-workflow.md). Git-only awareness;
# nothing here is a dependency.
#
# reports_through: the date of the newest process report (notes/fairyfox-reports/)
# this hub has already digested for that project, so the report-review pass knows
# where to resume. Updated by the review pass — see hub/standards/process-reports.md.
# null = none digested yet.

pokered-save-editor-2:
  commit: ef9578756201426b5815df49db4503265bb43c18
  branch: dev
  seen: 2026-06-26
  reports_through: 2026-06-26   # 2026-06-26-adopting-updates.md digested → folded into standards 0.9.1
  # Covered through the project-side fairyfox-mesh onboarding (mesh-aware CLAUDE.md +
  # cross-project-sync adoption) and theming its Doxygen docs site to the fairyfox
  # design system (Case B — themed generator). Folded into the 2026-06-25 day post
  # (_posts/2026-06-25-nodes-that-check-the-hub-for-updates.md). Earlier static-analysis
  # gate / test push was blogged at _posts/2026-06-23-…. (dev was force-pushed; re-cloned.)
  # 2026-06-26 round-up (later pass): new commit ef95787 — adopted the fairyfox git-flow,
  # process-reports & compliance standards (docs/notes/CI-comment only, no VERSION bump,
  # still 0.14.2-alpha). Blogged in _posts/2026-06-26-…. Marker advanced f88bbfd → ef95787.

random-ai-prompt:
  commit: 53fe12d13aaf8b9666d38ea88753747ced04e93a
  branch: dev
  seen: 2026-06-26
  reports_through: 2026-06-26   # 2026-06-26-adopting-updates.md digested → folded into standards 0.9.1
  # Covered through: project-side mesh onboarding + fairyfox-themed JSDoc docs, the
  # SFW-by-default + top-bar NSFW toggle (2.6.1), and adopting the git-flow standard
  # with the master → main rename (CI/Pages/release repointed). All folded into the
  # 2026-06-25 day post. (dev was force-pushed; re-cloned.)
  # 2026-06-26 round-up (later pass): dev force-pushed again, re-synced via fetch +
  # reset --hard on the mirror. Cut 2.7.0 (MINOR) — the repo split into engine-v3 (active)
  # + engine-v1-2 (frozen pre-revival snapshot), v3-only, legacy <expansion> mechanism
  # removed end-to-end — and adopted the process-reports + compliance standards (notes-only).
  # Blogged in _posts/2026-06-26-…. Marker → 53fe12d, VERSION 2.6.1 → 2.7.0.
