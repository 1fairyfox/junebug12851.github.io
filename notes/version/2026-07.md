# Changelog — July 2026

Newest on top. See [`../version.md`](../version.md) for the inline rule (each
entry ships inside the commit it describes).

### 2026-07-02 — Maintenance round-up: reconcile to RAP 2.38.1 + Games 0.6.0, blog the 1st (0.11.2)

Routine "usual stuff" pass. PowerShell + Read/Write/Edit only (no bash). Tree clean on `dev`
beforehand bar the untracked `notes/fairyfox-reports/inbound/INBOX-*.md` briefings (left untouched —
inbound report-review's job, not this pass's).

**References refreshed.**

- **pokered-save-editor-2** — `fetch` + `merge --ff-only` → "Already up to date" at `1bae326` (still
  `0.14.2-alpha`). No change; nothing to reconcile or blog.
- **random-ai-prompt** — clean fast-forward `c38373d → 2ed5787`, VERSION `2.35.1 → 2.38.1` (all commits
  dated 2026-07-01). No force-push.
- **fairyfox-games** — clean fast-forward `3cd3314 → 58a3bb0`, VERSION `0.5.1 → 0.6.0` (commit dated
  2026-07-01). A sixth game, Skyline.

**What the siblings shipped (2026-07-01).**

- **RAP 2.35.2–2.38.1.** A floor-up **CSS overhaul + theming framework** (Phases 0–7): the monolithic
  `styles.css` split into a modular `styles/` tree on a two-tier token system + cascade layers, a
  **System/Dark/Light** theme engine, **9 accent presets**, a header **Appearance picker**, built-in
  themes as a folder of JSON files, and **user theme import/export** (2.35.4→2.37.0). A **web-perf**
  pass — prompt corpus off the first-paint path (2.37.1), then the online build **prerendering its
  first paint to static HTML + hydration** (2.38.0; mobile Lighthouse Perf mid-50s→80s, A11y/BP/SEO
  stay 100). A **SonarCloud tech-debt sweep** — all **70 code smells** cleared incl. 7 ReDoS regex
  rewrites + 7 complexity refactors (2.38.1), the last bug/2 vulns resolved, and the quality-gate
  coverage measurement fixed. Plus a full dependency upgrade (2.35.2), a stylelint CSS gate (2.35.3),
  and a git-workflow back-merge (main→dev) fix + branch-sync guard.
- **Fairy Fox Games 0.6.0.** A mechanically-distinct sixth game **Skyline** (stack-and-slice tower
  builder; pure core + 18 tests, no timer-driven death), **Polarity** grown with clutch saves, and a
  backfilled Ricochet footer link. 133 tests green.

**Reconciled (changed):** `_data/projects.yml` (RAP → 2.38.1 / Jul 1; games → 0.6.0 / Jul 1; the
fairyfox.io meta card → 0.11.2 / Jul 2), `_data/pulse.yml` (ticker re-led with the 07-01 highlights,
`when` rolled, bars rolled), `_projects/random-ai-prompt.md` (added the themeable interface + online
prerender note), `_projects/fairyfox-games.md` (added Skyline as the sixth game).

**Verified accurate, left untouched:** `_data/downloads.yml` (RAP is source/run-only — no new pinned
binaries; PSE2/PSE1 releases unchanged; games has no downloads), `hub/registry.yml` (no version fields),
`_data/doc_categories.yml`, the `_docs/*` per-project pages (version-agnostic), `about.md` (lifecycle-based,
no hardcoded versions/counts), header/footer nav (registry-driven).

**Blogged.** `_posts/2026-07-01-a-theming-system-a-faster-first-paint-and-a-sixth-game.md` (tags
`random-ai-prompt, fairyfox-games, update`), day-centric. The site's own 07-01 change was the routine
maintenance bump (0.11.1) — not a notable feature — so no `site` tag this day. Markers advanced in
`hub/.last-seen.yml` (RAP → `2ed5787`; games → `58a3bb0`; PSE2 unchanged; all `seen` → 2026-07-02).
`reports_through` left alone — the new sibling process reports (RAP ×3) are for the inbound report-review
pass, not the round-up.

**Notes/version.** This changelog entry; `VERSION` `0.11.1 → 0.11.2`; today's session log; `status.md`
version line + sibling version numbers refreshed. Build-checked before shipping.

### 2026-07-01 — Maintenance round-up: reconcile to RAP 2.35.1 + Games 0.5.1, blog the 30th (0.11.1)

Routine "usual stuff" pass. PowerShell + Read/Write/Edit only (no bash). Tree was clean on
`dev` beforehand — the only untracked files were the `notes/fairyfox-reports/inbound/INBOX-*.md`
briefings from the automated inbound report-review procedure, left untouched (not this pass's
job).

**References refreshed.**

- **pokered-save-editor-2** — `fetch` + `merge --ff-only` → "Already up to date" at `1bae326`
  (still `0.14.2-alpha`). No change; nothing to reconcile or blog.
- **random-ai-prompt** — clean fast-forward → `c38373d`, VERSION `2.28.18 → 2.35.1` (all commits
  dated 2026-06-30). No force-push.
- **fairyfox-games** — clean fast-forward → `3cd3314`, VERSION `0.4.0 → 0.5.1` (all commits dated
  2026-06-30). A fifth game (Ricochet) plus a depth pass across the rest.

**What the siblings shipped (2026-06-30).**

- **RAP 2.29.0–2.35.1.** Self-hosted **Privacy/Terms/Cookies** pages + local fonts ending the
  IP-to-Google flow (2.30.1); a **real release stage** for the local edition (`npm start`, backend
  factored out of the Vite dev plugin) + file-watch hot reload + atomic settings writes (2.32.0);
  **in-app dialogs** replacing native alert/confirm/prompt (2.34.0); a **seedable, deterministic
  engine** (real threaded PRNG, reproducible-from-seed) + async batch boundary (2.35.0); a **README
  revamp + SEO repositioning** away from SD-first and a plain-English **description alignment**
  everywhere (2.33.3/2.35.1); a header/provider UX batch (2.30.0–2.32.1), natural-language
  artists/styles + a `{#styles}` block (2.33.0), a unified cookie-free storage overhaul (2.29.0),
  and CI-side security/code-health integrations (Dependabot/CodeQL/Scorecard/Sonar/CodeRabbit +
  Codecov, no bump).
- **Fairy Fox Games 0.4.1–0.5.1.** A mechanically-distinct fifth game **Ricochet** (aim-and-fire one
  bouncing shot; pure `computeShot` core + tests), one on-mechanic depth/feedback addition to every
  game, landing-page polish, the retired "Fun" surface removed, and first-class-games standing rules.
  110 tests green.

**Reconciled (changed):** `_data/projects.yml` (RAP → 2.35.1 / Jun 30 + blurb aligned to the new
positioning; games → 0.5.1 / Jun 30; the stale **fairyfox.io meta card 0.9.14 → 0.11.1** / Jul 1),
`_data/downloads.yml` (RAP run command `npm run web` → `npm start`, the proper release stage added in
2.32.0), `_data/pulse.yml` (ticker re-led with the 06-30 highlights + `when` rolled + bars rolled),
`_projects/random-ai-prompt.md` (seedable/deterministic engine + privacy-by-design stance),
`_projects/fairyfox-games.md` (added Ricochet + the grow-daily note).

**Verified accurate, left untouched:** `_data/downloads.yml` PSE2/PSE1 tabs (unchanged releases),
`hub/registry.yml` (no version fields), `_data/doc_categories.yml`, the `_docs/*` per-project pages
(version-agnostic), `about.md`, header/footer nav (registry-driven).

**Blogged.** `_posts/2026-06-30-legal-pages-a-seedable-engine-and-a-fifth-game.md` (tags
`random-ai-prompt, fairyfox-games, fairyfox-io, site, update`), day-centric — also folds in the site's
own 2026-06-30 work (the 0.10.0 one-seamless-site standards milestone + the 0.11.0 submenu nav).
Round-up markers advanced in `hub/.last-seen.yml` (RAP → `c38373d`; games → `3cd3314`; PSE2 unchanged;
all `seen` → 2026-07-01). New sibling process reports left for the inbound report-review pass.

**Notes/version.** This changelog entry + July file created (index row added to `version.md`); today's
session log; `VERSION` `0.11.0 → 0.11.1`; `status.md` version line + sibling version numbers refreshed.
Build-checked before shipping.
