# 2026-06-22 — Session Log

## List the predecessor — Pokered Save Editor (v1)

Added the original `pokered-save-editor` (Electron/Angular, Apache-2.0) — the
predecessor to `pokered-save-editor-2`. It's complete and stable, so it's the
recommended working editor until the Qt 6 rewrite reaches parity; it follows none
of the shared standards and is not part of the sync mesh.

- Added matching entries to `_data/projects.yml` (featured, `docs: null`,
  `reference: false`) and `hub/registry.yml` (`branch: master`, `adopts_hub` /
  `notes_system` false) — kept in step, and `reference: false` keeps it out of
  `assets/references/`.
- New `_docs/pokered-save-editor.md` (projects, order 2), cross-linked both ways
  with the v2 page. PATCH bump `0.2.0 → 0.2.1`.

## Reframe as documentation hub + dropdown nav — neutral voice, Fairy Fox identity

Direction change after review. The owner is **Fairy Fox** (not Twilight); the site
should read as a project hub + documentation library in a neutral, professional
voice (the assistant's voice, not first-person-as-owner, no glorifying, no puns),
while keeping the existing colour/theme.

- Switched all site/notes references to **Fairy Fox**; recorded the voice rule in
  `context/principles.md` and `../../CLAUDE.md`.
- Rebuilt the header: gradient `FF` monogram + wordmark, **dropdown menus**
  (Projects, Docs) + mobile hamburger (`assets/js/nav.js`), keeping the palette.
- Added an on-site **documentation library**: `_docs` collection + `doc` layout
  with a sidebar, `/docs/` landing, and pages for the ecosystem overview, the five
  shared standards, and the two projects.
- The Projects menu links into each project's own Pages docs site. Confirmed via
  `gh` that GitHub now serves the project sites under the custom domain
  (pokered-save-editor-2 → `https://fairyfox.io/pokered-save-editor-2/`).
  random-ai-prompt has no Pages site yet (links to its repo/README).
- Rewrote home/projects/about/intro-post in the neutral voice. Bumped `0.2.0`.

Custom domain: DNS A-records resolve to GitHub Pages; HTTPS cert still being
provisioned by GitHub (Enforce HTTPS pending — to enable once the cert exists).

## Project created — fairyfox.io home page + cross-project hub

Built the project from near-scratch (empty repo folder, no GitHub repo yet).

**What was done**

- Studied two reference repos for their conventions — shallow-cloned
  `pokered-save-editor-2` and `random-ai-prompt` into `assets/references/`
  (git-ignored). The first carries a mature project-management system (a living
  `notes/` tree, a `CLAUDE.md` AI-context file, SemVer via a `VERSION` file, an
  inline-changelog rule, an FF-only git workflow, and an Actions-based Pages
  deploy). This project mirrors that system.
- Scaffolded a clean custom Jekyll site (no external theme): `_config.yml`,
  `Gemfile`, layouts (`default`/`page`/`post`), includes (`head`/`header`/
  `footer`), custom responsive CSS with light/dark, home + projects + blog +
  about pages, a first post, and a favicon. Projects render from a single
  `_data/projects.yml` registry.
- Added the GitHub Actions Pages workflow (Bundler + Jekyll build → Pages), a
  `CNAME` for `fairyfox.io`, and a `.gitignore` that keeps build output and the
  reference clones out of git.
- Wrote the full `notes/` system and the `hub/` system (standards + templates +
  registry) and documented the pull-only cross-project sync model.

**Environment notes**

- GitHub account is `junebug12851` (git user.name "Fairy Fox"); `gh` is authed
  via SSH. So the user-site repo is **`junebug12851.github.io`**, with the
  `fairyfox.io` custom domain pointed at it.
- Ruby/Jekyll were not installed locally; installing Ruby 3.3 + DevKit via winget
  to allow a local build check. CI builds regardless.
- The Cowork bash sandbox is unreliable here (stale/truncated reads) — all shell
  work goes through PowerShell on the real machine; file work through the
  editor tools.

**Shipped**

- Installed Ruby 3.3.11 + DevKit; `bundle install` + `bundle exec jekyll build`
  green locally (dropped the `wdm` gem — it broke Bundler resolution on modern
  Ruby and is only an optional Windows watch speedup). Added `x86_64-linux` +
  `ruby` to `Gemfile.lock` platforms so CI installs cleanly.
- Created the public repo `junebug12851/junebug12851.github.io`, committed on
  `dev`, branched `main`, pushed both (SSH), set `main` default.
- Enabled Pages with `build_type=workflow`; the first `pages` run was green
  (build + deploy). **Site live at https://junebug12851.github.io/** — home,
  projects, blog, about, feed all serve (200).

**Next:** wire DNS for the `fairyfox.io` custom domain (registrar) + set it in
Settings → Pages. See [`../../plans/next-steps.md`](../../plans/next-steps.md).
