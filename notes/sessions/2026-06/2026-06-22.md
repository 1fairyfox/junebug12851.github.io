# 2026-06-22 — Session Log

## Project created — fairyfox.io home page + cross-project hub

Built the project from near-scratch (empty repo folder, no GitHub repo yet).

**What was done**

- Studied two reference repos for their conventions — shallow-cloned
  `pokered-save-editor-2` and `random-ai-prompt` into `assets/references/`
  (git-ignored). The first carries a mature project-management system (a living
  `notes/` tree, a `CLAUDE.md` AI-context file, SemVer via a `VERSION` file, an
  inline-changelog rule, an FF-only git workflow, and an Actions-based Pages
  deploy). This project mirrors that system.
- Scaffolded a clean custom Jekyll site (no external theme): `_config.yml`,
  `Gemfile`, layouts (`default`/`page`/`post`), includes (`head`/`header`/
  `footer`), custom responsive CSS with light/dark, home + projects + blog +
  about pages, a first post, and a favicon. Projects render from a single
  `_data/projects.yml` registry.
- Added the GitHub Actions Pages workflow (Bundler + Jekyll build → Pages), a
  `CNAME` for `fairyfox.io`, and a `.gitignore` that keeps build output and the
  reference clones out of git.
- Wrote the full `notes/` system and the `hub/` system (standards + templates +
  registry) and documented the pull-only cross-project sync model.

**Environment notes**

- GitHub account is `junebug12851` (git user.name "Twilight"); `gh` is authed
  via SSH. So the user-site repo is **`junebug12851.github.io`**, with the
  `fairyfox.io` custom domain pointed at it.
- Ruby/Jekyll were not installed locally; installing Ruby 3.3 + DevKit via winget
  to allow a local build check. CI builds regardless.
- The Cowork bash sandbox is unreliable here (stale/truncated reads) — all shell
  work goes through PowerShell on the real machine; file work through the
  editor tools.

**Next:** create + push the GitHub repo, confirm the first Actions deploy is
green, wire DNS for the custom domain. See [`../../plans/next-steps.md`](../../plans/next-steps.md).
