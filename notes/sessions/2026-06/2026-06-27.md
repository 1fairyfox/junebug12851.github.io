# 2026-06-27 — Session Log

## Scheduled maintenance round-up — reconcile to Random AI Prompt 2.7.25 (0.9.5)

Automated "usual stuff" pass. `main` at `v0.9.4`, tree clean on `dev` (one pre-existing untracked
inbound briefing, `notes/fairyfox-reports/inbound/INBOX-2026-06-26-1803.md`, from an earlier
report-review pass — left as-is; not a live-session collision). PowerShell + the editor tools
throughout (no bash).

**References.** Both git-ignored mirrors **fast-forwarded cleanly** (`git fetch` + `merge --ff-only`;
no force-push this cycle, no re-clone fallback needed).

- **random-ai-prompt** — `53fe12d → 1868b58`. The **2.7.x line (2.7.0 → 2.7.25, all dated 2026-06-26)**
  built out the engine-v3 web app's **image-generation layer**: a finished prompt can be dispatched to
  16+ providers — hosted APIs and local servers (OpenAI, Stability, Replicate, fal.ai, Gemini, Grok,
  FLUX/BFL, Ideogram, Leonardo, NovelAI, Midjourney, ComfyUI, Forge/SD.Next, and a plain copy-out),
  each capability-described so the settings panel adapts — plus an optional AI rewrite pass, a central
  `output/` folder, and a **photo gallery + dedicated single-image page** over three persistent
  Generate/Gallery/Single views, with per-image `.json` metadata sidecars (prompt/roll/AI/DPL/provider/
  settings, **API keys stripped**). Local-by-nature (the feed + ImageMagick convert need the dev
  server). Also re-synced the hub's 0.9.2/0.9.3 standards deltas (commits `f0cf652`/`0cd35ce`, no
  version bump). Round-up commit marker advanced.
- **pokered-save-editor-2** — `3ff32c6 → 1bae326`: a single fairyfox **check-for-updates** pass against
  hub 0.9.4, no change to adopt (still `0.14.2-alpha`). No-op docs/notes; nothing blog-notable. It also
  wrote a new `notes/fairyfox-reports/2026-06-26-check-for-updates.md` — that's for the inbound
  report-review pass, not this round-up; `reports_through` left alone. Marker advanced.

**Reconciled.** `_data/projects.yml` (RAP `2.7.0 → 2.7.25` + blurb extended to the image-provider +
gallery capability; fairyfox meta `0.9.4 → 0.9.5`, Jun 27), `_data/pulse.yml` (re-aged one day — the
prior "today" 2026-06-26 batch dropped to `1d`; ticker re-led with the four provider/gallery highlights;
bars rolled forward, showing the big 2026-06-26 spike and a small today), the RAP node page
(`_projects/random-ai-prompt.md`), and the About page RAP line. Also fixed genuine **doc drift**:
`_docs/random-ai-prompt.md` still listed the removed `expansion` mechanism and framed the CLI as
current — rewritten to the DPL + web-app + image-provider reality, CLI noted as frozen in `engine-v1-2`.

Verified already-accurate and left untouched: `_data/downloads.yml` (RAP still source/run-only; the
`npm run web` script still resolves after the earlier `web-app → gui` rename — it delegates to `gui`),
`hub/registry.yml` (no versions; flags accurate), the other three node pages, `_data/doc_categories.yml`,
the rest of the `_docs/` library, header/footer.

**Blogged.** Folded the provider/gallery build-out into the existing **2026-06-26 day post** (all the
work landed that calendar day, after the prior round-up ran) — new section "The prompt tool starts
generating images"; subtitle extended through 2.7.25. One post per day. Neutral voice; the SD-capable
backends (Stability, ComfyUI, Forge/SD.Next) are framed as pluggable providers, not as the tool's
identity — no Stable-Diffusion framing of Random AI Prompt itself. No booster tags.

Bumped `VERSION` + meta row + `status.md` to `0.9.5`. Build-checked; shipped PATCH `0.9.4 → 0.9.5`
direct `dev → main`.
