# 2026-06-23 — Session Log

## Scheduled maintenance pass — refresh references, round-up, refresh downloads

Routine "the usual stuff" pass.

- **References refreshed.** Both sibling `dev` branches (`pokered-save-editor-2`,
  `random-ai-prompt`) had been **history-rewritten upstream** (force-pushed), so the
  `--ff-only` shallow pull aborted on both. Re-cloned each git-ignored shallow clone fresh
  instead. New `dev` heads: pokered-save-editor-2 `005a0cb`, random-ai-prompt `8fd02ab`.

- **Round-up written (pokered-save-editor-2).** Last post for it was 2026-06-17; its
  0.14.x work (all dated 06-22 in its changelog) was unblogged. New post
  `_posts/2026-06-23-pokered-save-editor-2-static-analysis-and-tests.md` covers the new
  clang-tidy/cppcheck/qmllint static-analysis gate and the real defects it caught — the
  `expLevelRangePercent()` integer-division bug (fractional percent truncated to 0 until
  the top of a level) and the shadowed-parameter `ppUp = 0` dead store that left new moves
  with random PP-Ups — plus the test-type breadth push (signal/slot via `QSignalSpy`,
  `QAbstractItemModelTester` model contracts, a blank-render visual-regression floor, and
  Given/When/Then acceptance scenarios).

- **random-ai-prompt: nothing new to blog.** Its only commit since yesterday's
  "tests-and-shipping" post is incremental CI plumbing (commit Linux visual baselines, run
  visual-regression in CI) — closes a gap noted in that post, not its own story. Skipped.

- **Downloads refreshed.** Both projects cut releases today. `_data/downloads.yml`
  re-pinned pokered-save-editor-2 from **v0.13.1-alpha → v0.14.2-alpha** (verified the four
  artifact filenames on the GitHub release). random-ai-prompt's v2.6.0 release has only a
  source tarball + docs zip (no binaries), so its source/run-only entry is unchanged.

- **Marker added.** `hub/.last-seen.yml` now records the `dev` commit each project was at
  for this pass, per the blogging workflow.

- VERSION 0.2.5 → 0.2.6 (PATCH). Build-checked green, shipped on `dev`, FF'd to `main`.
