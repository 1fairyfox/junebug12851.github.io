# 2026-07-23

_Newest entry on top._

## Process-report review pass + fold-back → standards 0.21.0

Ran the inbound **report-review pass** (`hub/standards/process-reports.md`) on an explicit
*fairyfox*-named request with an upfront go-ahead to carry the fold to completion.

**Check + read.** Refreshed all five reference clones (pse2 + random-ai-prompt already current;
papermc-despawned-items → `f3a2d01`/v1.5.0, fairyfox-games → `898a6de`/0.25.2, fairyfox-stories →
`ecc53f9`/0.7.1). Read **15 reports** past their `reports_through` markers. Hub clean on `dev` at
0.20.7 before starting — no concurrent live session.

**Synthesis.** The dominant, owner-confirmed pattern: "whole checklists get skipped while marked
done" — the mesh's "done = artifact exists" vs. the standards' "done = every `## Verify` passes",
with `compliance.md` opt-in and never invoked. Same disease in adoption, owner-mandates, and testing.
Plus cross-node patterns: no standards/bundle version stamp (3 nodes), coins-inside-chrome coupling
(3), check-links false positives (2), agent-tooling gotchas (3), a JVM/Gradle/Dokka blind spot.
Full itemized ledger in [`plans/2026-07-23-report-review-foldback.md`](../../plans/2026-07-23-report-review-foldback.md).

**Fold (0.21.0, MINOR).** 26 items across the standards: 2 new standards
(`checklists-are-contracts`, `mandate-ledger`), the adoption-manifest enforcement pulse
(manifest template + adopt/onboard record Verify + release reads the manifest + report-review
spot-check), the ship contract, supply-chain/dependencies/testing hardening, the adaptive docs-site
subnav + Dokka adapter + chrome CHANGELOG, and a batch of coins/legal/repo-hygiene/agent-tooling/
onboarding clarifications + `check-standards.mjs` + a `branch-sync.yml` race fix + a
`hub/standards/CHANGELOG.md`. See the 0.21.0 changelog entry for the file-by-file breakdown.

**Markers + report.** Advanced every digested report in `hub/.last-seen.yml reports_through`
(including papermc-despawned-items' first eight). Wrote the hub's own process report
[`fairyfox-reports/2026-07-23-report-review.md`](../../fairyfox-reports/2026-07-23-report-review.md)
— fairyfox.io is a node too.

**Deferred (honest not-done):** self-hosting the docs fonts *inside* the chrome bundle (pse2's
standing ask — a bundle-content change deserving its own pass, not this standards fold); an
auto-generated one-page index of every `## Verify` table (a stretch beyond the `standards/CHANGELOG.md`
that shipped). Both recorded in the plan as `gap(next pass)`.
