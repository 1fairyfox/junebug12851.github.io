# 2026-07-05

_Newest on top._

## Shared "Aa" reader menu + cards straight to the project — 0.13.0

A MINOR milestone (feature branch `feature/seamless-projects-and-reader` off `dev`). Live human
session; tree was clean on `dev` beforehand apart from the untracked inbound `INBOX-*.md`
briefings (left untouched). Plan: [`../../plans/2026-07-05-seamless-projects-and-reader.md`].

**Why.** Fairy Fox pulled RAP's refreshed docs theme (its modular ES-module + CSS-module
rebuild) and wanted its **Kindle-style "Aa" reader** shared across fairyfox.io + all
sub-projects (origin-wide `fairyfox:reader` key), and the hub's **local project "node" pages**
gone — a card should open the project's own page directly (same chrome + sub-navbar), since each
project explains itself.

**Reader feature.** `main.css` → `data-theme` model (OS light query guarded with
`:not([data-theme])`), a real **Sepia** theme, `--reading-*` vars on the reading surfaces, and
the `.ff-reader-*` button/panel styles. New `assets/js/reader.js` (same key + constants as RAP,
so a choice is interchangeable); a no-flash inline apply in `head.html`; `reader.js` loaded in
`default.html`.

**Node pages retired.** Deleted `_projects/*.md` + `_layouts/project.html`; dropped the
`projects` collection from `_config.yml`; `project-card.html` + `subnav.html` now target
`p.docs` (→ `fairyfox.io/<key>/`) with a repo fallback; `about.md` links repointed; dead
node-page CSS removed.

**Standard in step.** `hub/standards/docs-site/` 02/04/05/08/11 + `reference/main.css` +
`reference/chrome.html` document the reader as required (origin-wide key + Sepia) and record the
node-page retirement.

**Verified.** `JEKYLL_ENV=production jekyll build` green; served the build locally and screenshotted
home, `/projects/`, and theme-test pages in **light / sepia / dark** with the reader panel open —
all clean, Aa button in the header, cards showing "Open → · Repository ↗". Temp artifacts removed.

## Maintenance round-up — reconcile to RAP 2.42.1 + Games 0.13.1, blog the 4th (0.12.4)

Routine "usual stuff" pass (PowerShell + Read/Write/Edit only; no bash). Tree clean on `dev`
beforehand apart from the untracked `notes/fairyfox-reports/inbound/INBOX-*.md` briefings, left
untouched (inbound report-review's job, not this pass's).

**References refreshed** (git-ignored clones under `assets/references/`, `fetch` + `merge --ff-only`):

- **pokered-save-editor-2** — "Already up to date" at `1bae326`, still `0.14.2-alpha`. No change.
- **random-ai-prompt** — clean fast-forward `cdfaf7c → 5dcd621`, VERSION `2.41.0 → 2.42.1` (Jul 4).
- **fairyfox-games** — clean fast-forward `1994e12 → 0a8db92`, VERSION `0.12.0 → 0.13.1` (Jul 4).

**Siblings' 2026-07-04 work.**

- **RAP 2.42.1.** The big one is **2.42.0** — a large-scale performance pass built to a stated ceiling
  (100k gallery, 1000 prompts, 10k images, 100k catalog, all at once): a **virtualized** gallery, a bounded
  results list, **placeholder-first chunked** generation with per-provider concurrency limiters (a new
  folder-based shared-settings system), and a perf test suite. Plus an explicit **Random-seed control**
  (2.41.2, replacing a `-1` sentinel that pinned every roll), seed-preview independence + zero Sonar debt
  (2.41.3), a mobile gear-backdrop fix + a "regression test with every fix" standing rule (2.41.1), and a
  dark-mode DPL autocomplete contrast fix + an auto per-release screenshot/GIF pipeline (2.42.1).
- **Games 0.13.1.** A ninth game **Symmetry** (mirror coordination) + a near-miss line on Poise (272 tests),
  and a release-policy change: ship `dev → main` by default on green.

**Reconciled:** `_data/projects.yml` (RAP → 2.42.1 / Jul 4, games → 0.13.1 / Jul 4, site meta → 0.12.4 / Jul 5),
`_data/pulse.yml` (ticker + `when` + bars rolled), `_projects/fairyfox-games.md` (added Symmetry).
**Verified accurate, left alone:** `downloads.yml`, `_projects/random-ai-prompt.md`, `hub/registry.yml`,
`doc_categories.yml`, all `_docs/*`, `about.md`, nav.

**Blogged** `_posts/2026-07-04-a-ninth-game-and-prompts-that-scale.md` (tags `fairyfox-games, random-ai-prompt,
update`). Advanced `hub/.last-seen.yml` markers (RAP → `5dcd621`, games → `0a8db92`, PSE2 unchanged; `seen` →
07-05). New RAP report `2026-07-04-propose-regression-testing-standard.md` left for the inbound report-review pass.

Build-checked with `JEKYLL_ENV=production bundle exec jekyll build` (green), then shipped `dev` → ff `main`.
VERSION `0.12.3 → 0.12.4`.
