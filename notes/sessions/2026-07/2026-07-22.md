# 2026-07-22

## Onboard Minecraft Automated Driver (hub-side) + rename category → "Minecraft Projects" (0.20.7)

A **live owner session** — distinct from the earlier scheduled 0.20.6 round-up on this same day.
Started from a clean tree at 0.20.6, up to date with `origin/dev`. Owner asked to register a new
sub-project, **minecraft-automated-driver**, then mid-session asked to rename the "PaperMC Plugins"
group to **"Minecraft Projects"** (the driver "does use client- and server-side plugins", so it
fits the group) and to fix the lone-card full-width bug in that group.

**What it is.** An MCP server (Node/JS host at the repo root; Kotlin in-game agents from Phase 3)
that drives the whole Minecraft dev loop: build/test plugins + mods, boot Paper servers, spawn or
attach clients, screenshots, Mineflayer bots, and gated loopback-only semantic control of a live
client/server. Pre-alpha — `v0.1.2`, Phase 0 (scaffold + quality gates) complete on `main`. Its own
`notes/status.md` already named exactly this hub paperwork (and the group rename) as the missing
piece.

**Hub-side changes (this repo only):**

- `_data/projects.yml` — new `Minecraft Automated Driver` card (category `minecraft`, alpha, v0.1.2,
  accent `#43a047`, no icon yet, `docs: null` → repo fallback); DespawnedItems moved `papermc →
  minecraft`; the category comment block retitled; meta card `→ 0.20.7`.
- `hub/registry.yml` — new `minecraft-automated-driver` row, **`adopts_hub: false` /
  `notes_system: true`** (honest: no themed docs site yet, Phase 7); the despi comment's category
  reference updated.
- `projects.md` — `where` query `"papermc" → "minecraft"`, `<h2>` → "Minecraft Projects", prose
  broadened (plugins + tooling), category grid gains the `lean` class.
- `assets/css/main.css` — new `.grid.cols-2.lean` / `.grid.cols-3.lean` (`auto-fill`) so a 1–2 card
  section doesn't balloon.
- `notes/status.md`, `_data/pulse.yml` — current-state prose + a new ticker highlight.

**Registered ≠ integrated.** Per the onboarding runbook this is the **hub-side registration** step
only. The project-side work (mesh `CLAUDE.md`, notes, git model, gates) is already done by the
project itself, but the themed docs site is not — so the mesh state is **partial**, recorded
honestly in the registry flags. No project-side files were touched here.

**Follow-ups (not blockers):** a `minecraft-automated-driver` card icon (renders an empty glyph
until then); the themed docs site at `fairyfox.io/minecraft-automated-driver/` (project-side, its
Phase 7) — until it lands, flip `adopts_hub` to `true`.

**Ship.** Build-checked (`jekyll build`), committed on `dev`, released `dev → main` (PATCH, `--no-ff`,
tagged `v0.20.7`), back-merged to `dev`. VERSION `0.20.6 → 0.20.7`.

## Maintenance round-up — the 21st blogged; sequel + Drove + the coverage-truth day reconciled (0.20.6)

A scheduled maintenance pass. Started clean on `dev` at 0.20.5, tree clean and up to date with
`origin/dev` (no concurrent live-session collision to work around).

**References.** All five sibling mirrors fast-forwarded cleanly (no force-pushes). **Random AI
Prompt** (`35b1370`, still `2.60.1`, last commit 07-19) and **Pokered Save Editor 2** (`b3128652`,
still `0.43.6-alpha`, last commit 07-20) were both unchanged. The other three moved, every new
commit dated **2026-07-21** — one complete day (today is 07-22) — so one day post:

- **DespawnedItems** `2e24083 → 2cb470d`, `1.3.4 → 1.4.8`, 44 commits, ten passes. The headline is
  that the plugin's own health claim was the bug: "~49 tests ✅" measured out at 44.2% line / 20.7%
  branch, and closing it to ~95.6% / ~78.6% (koverVerify min 90 now fails the build) surfaced
  **three shipped bugs** in previously-0% layers. Then the test *layers* got automated — a real
  Paper server booted in CI with a Mineflayer bot joining as a player, a 123-node permission matrix,
  combined load + throughput proofs, Testcontainers MariaDB, Kotest/Pitest/JMH — alongside the
  ship-contract mandate (8 Dependabot PRs handled with judgment; blind-merging would have shipped a
  CodeQL-breaking Kotlin bump and an out-of-sync runtime `libraries:` list), Scorecard 6.9 → 7.1,
  `/despi recycle` + renameable commands, self-hosted legal pages, and a rolling latest-Paper smoke.
  Marker `→ 2cb470d`.
- **Fairy Fox Games** `015a421 → 793c32b`, `0.24.12 → 0.25.1`. Repo-hygiene scripts adopted
  (0.24.13); **Loft** joined the depth layer at 8 of 13 (0.24.14); **Drove** planted as the 16th game
  and the collection's first *indirect-control* verb — a verb survey confirmed all 15 existing verbs
  act on the world directly (0.25.0, 662 → 704 tests); and a hotfix renamed `games/_shared` →
  `games/shared` after Jekyll silently omitted the underscore-prefixed directory holding the shared
  coins module every game imports (0.25.1). Marker `→ 793c32b` (HEAD, a 07-21 commit).
- **Fairy Fox Stories** `6e25309 → 7a1951d`, `0.6.0 → 0.7.0`, shelf 8 → 9 books. The **weekly sequel
  roll fired for the first time ever, and twice in one draw** (Launderette 13, Girl Who Sold the Wind
  9, on ≤25) — passing over the one series that had pre-planted its own hook. Staleness broke the
  tie, and *The One-O'Clock Bus* planted as the farm's first sequel: first `sequelTo` manifest, first
  three-book realm (The Hollow Hours), and an author roll that pointedly returned a *different*
  writer, which enforces the farm's own "new tale in the realm, not a continuation" rule. Plus the
  daily grow: *Every Lock but Hers* ch.6, its moral hinge. Marker `→ 7a1951d`.

**Blogged.** New day post
`_posts/2026-07-21-a-first-sequel-a-first-indirect-verb-and-tests-that-told-the-truth.md` — the
farm's dice finally say yes (twice); a verb that never touches what it moves; and a coverage number
that turned out to be the bug. Tags `[papermc-despawned-items, fairyfox-games, fairyfox-stories,
update]`. RAP and PSE2 not tagged (no 07-21 commits). No `[site]` strand this time: the site's own
0.20.5 was the previous maintenance pass itself, which is routine, not notable.

**Reconciled.** `_data/projects.yml` — DespawnedItems `→ 1.4.8 / Jul 21` (blurb gains renameable
commands + the CI-gated ~95% suite with its real server boot and in-game bot), Games `→ 0.25.1 /
Jul 21`, Stories `→ 0.7.0 / Jul 21`, meta card `→ 0.20.6 / Jul 22`; RAP and PSE2 left as they were.
`_data/pulse.yml` — three 07-21 highlights added as "today", priors shifted a day, the three oldest
dropped, bars advanced. `notes/status.md` — the stale sibling version anchors refreshed (Games
`0.23.1 → 0.25.1` with the Drove/depth-8-of-13 note, Stories `0.4.1 → 0.7.0` with the first-sequel
note, PSE2 `0.41.8 → 0.43.6`, DespawnedItems `v1.3.4 → v1.4.8`). `hub/registry.yml`, the `_docs`
library + `_data/doc_categories.yml`, About and header/footer — checked, all accurate, no change
(no project added or removed; every hub standard still has its `_docs` page; RAP, the mesh's only
standards-bearing sibling, was quiet, so no hub standard moved). `_data/downloads.yml` and
`_projects/` do not exist — both retired in 0.13.x — so those steps are permanently N/A here.
`hub/.last-seen.yml` — three markers advanced, two `seen` dates advanced, five round-up notes added.

**Left for the inbound report-review pass** (not this one): four new DespawnedItems process reports
dated 2026-07-21 — mandate-execution failure analysis, testing-and-quality gap analysis, a
ship-contract/repo-hardening proposal, and a docs subnav standard proposal. Three of the four
propose amendments to the shared standards, so the next report-review has real material.

**Ship.** Build-checked, committed on `dev`, released to `main`. VERSION `0.20.5 → 0.20.6`.
