# 2026-07-14 — Maintenance round-up: the 13th blogged (0.15.11)

_Newest entry on top. See [`../README.md`](../README.md) for the system._

## Round-up pass — PSE2 0.34.0, Games 0.22.1, Stories 0.3.5

Scheduled maintenance pass. Working tree was clean on `dev` (bar the untracked
`notes/fairyfox-reports/inbound/INBOX-*` files, left alone — they belong to the inbound
report-review pass, not this one).

**References.** All four fast-forwarded cleanly, no force-pushes, no re-clone needed.

| Project | Marker | VERSION |
|---|---|---|
| pokered-save-editor-2 | `0686b58` → `dc1f21b` (20 commits) | `0.24.4-alpha` → `0.34.0-alpha` |
| random-ai-prompt | unchanged (`2f1459f`) | `2.60.1` (no commits on the 13th) |
| fairyfox-games | `e3f9a7f` → `dda05a9` | `0.22.0` → `0.22.1` |
| fairyfox-stories | `17fc7f3` → `a5ddca6` | `0.3.4` → `0.3.5` |

Every new commit across the three moving projects is dated **2026-07-13** — one complete day,
so one day post.

**The day, in one line each.** PSE2 spent it under review, and the review kept finding that the
*instrument* was blind: screenshots with no text in them (the headless capture never set a font
dir), a test harness whose "click" emitted a signal rather than delivering a pointer event, four
sprite enums writing the opposite of what they said, and a music format the project had invented
and shipped without the source it was built from. Along the way the towns filled with people (all
72 sprites) and the cast became editable with the mouse. Games brought **Loft** onto varied
structure by varying *the air* rather than the spawn. Stories grew three chapters and logged an
unlucky-looking two-of-three random-override day honestly.

**Reconciled.**

- `_data/projects.yml` — PSE2 → `0.34.0` / Jul 13 (blurb now notes the map's editable cast);
  games → `0.22.1` / Jul 13; stories → `0.3.5` / Jul 13; RAP left at `2.60.1` / Jul 12; the site's
  own meta row → `0.15.11` / Jul 14.
- `_data/pulse.yml` — bars shifted one day (24 commits on the 13th); ticker rewritten, newest first.
- `hub/.last-seen.yml` — markers + round-up notes for all four (RAP: an explicit no-change note).
- `hub/registry.yml`, the `_docs/` library, `_data/doc_categories.yml` and the About page — **checked,
  already accurate, no change** (no hub standards moved this cycle; no site content hardcodes a
  sibling's version).

**Blogged.** `_posts/2026-07-13-the-screenshots-had-no-text-in-them.md` — tags
`[pokered-save-editor-2, fairyfox-games, fairyfox-stories, update]`. RAP is deliberately *not*
tagged: it did nothing on the 13th. No `site` tag either — the hub's own commit that day was this
maintenance pass's predecessor, which is not notable.

**Build:** `JEKYLL_ENV=production bundle exec jekyll build` green. Released `0.15.11` (PATCH).
