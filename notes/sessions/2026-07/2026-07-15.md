# 2026-07-15

## Maintenance round-up pass — reconcile + blog the 14th (0.15.12)

A scheduled maintenance pass. Refreshed all four sibling references, reconciled the data-driven
components against reality, blogged the one complete day found (2026-07-14), and shipped a PATCH.

**References refreshed** (all clean fast-forwards, no force-pushes):

- **pokered-save-editor-2** `dc1f21b → ff38b460`, VERSION `0.34.0 → 0.39.0-alpha` — 12 commits, all
  dated 2026-07-14. The map screen gained its object types: warps (Phase 5, `0.36.0`), signs
  (Phase 6, `0.38.0`), and the full player details panel (Phase 5f, `0.39.0`), each preceded by
  console-verified research; plus the tile-anim + walk controls merged into one Simulation control
  (`0.37.2`). Notable non-code moment: signs were caught walking into the warps phase and cut, with a
  new "adjacency is not a brief" rule added to the project's CLAUDE.md.
- **random-ai-prompt** — unchanged at `2f1459f` (`2.60.1`); no commits dated 2026-07-14.
- **fairyfox-games** `dda05a9 → 3c4855a`, VERSION `0.22.1 → 0.22.2` — Poise onto varied structure,
  **completing the rollout at 13 of 13**.
- **fairyfox-stories** `a5ddca6 → 2e953ae`, VERSION `0.3.5 → 0.3.6` — **first book complete** (The
  Two-O'Clock Launderette, 3/3); Cinderwick ch.4, Every Lock but Hers ch.3; two new characters.

**Components reconciled:**

- `_data/projects.yml` — PSE2 → `0.39.0` / Jul 14 (blurb now notes doors + signs + player state on the
  map view); games → `0.22.2` / Jul 14; stories → `0.3.6` / Jul 14; RAP left at `2.60.1` / Jul 12; the
  meta fairyfox.io row → `0.15.12` / Jul 15.
- `_data/pulse.yml` — bars shifted one day (appended the 2026-07-14 count); ticker rebuilt so the
  2026-07-14 work is "today" and older entries aged out.
- `_data/downloads.yml` — still retired (does not exist); N/A.
- `_projects/` node pages — still retired (dir does not exist); N/A.
- `hub/registry.yml`, the `_docs/` library, `_data/doc_categories.yml`, and `about.md` — checked,
  accurate, no change (no hub standards changed this pass; RAP, the only standards-bearer that could
  have, was unchanged).
- `hub/.last-seen.yml` — markers advanced to `ff38b460` / `2f1459f` (unchanged) / `3c4855a` /
  `2e953ae`; all `seen` → 2026-07-15, each with a round-up note.

**Blogged:** `_posts/2026-07-14-what-it-means-to-finish-something.md` — one day-centric post
(tags `pokered-save-editor-2`, `fairyfox-games`, `fairyfox-stories`, `update`). RAP absent (no work
that day). The site itself shipped only this maintenance PATCH, which the round-up post + changelog
already cover, so no separate `[site, update]` post was warranted.

**Version:** `0.15.11 → 0.15.12` (PATCH). Build-checked green; shipped dev → main.

_Note: untracked `notes/fairyfox-reports/inbound/INBOX-*.md` files (dated late June–early July) were
present in the working tree and left untouched — not this pass's concern, and staging was file-specific._
