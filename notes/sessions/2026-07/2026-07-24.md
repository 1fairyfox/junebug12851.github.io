# 2026-07-24

_Newest entry on top._

## Maintenance round-up: reconcile the 22nd + 23rd, surface four new standards (1.5.1)

Scheduled site-maintenance pass. Clean tree on `dev` before and after staging.

**References refreshed** (git-ignored clones, plain fetch + `--ff-only`, all clean):

| Sibling | Marker → tip | VERSION | Result |
|---------|--------------|---------|--------|
| fairyfox-games | 793c32b → 78a9b05 | 0.25.1 → **0.25.3** | reconciled + blogged |
| fairyfox-stories | 7a1951d → 36307bd | 0.7.0 → **0.7.2** | reconciled + blogged |
| papermc-despawned-items | 2cb470d → f3a2d01 | 1.4.8 → **1.5.0** | reconciled + blogged |
| pokered-save-editor-2 | b3128652 (unchanged) | 0.43.6-alpha | no change |
| random-ai-prompt | 35b1370 (unchanged) | 2.60.1 | no change |

**What actually shipped in the siblings (22nd + 23rd):**

- **DespawnedItems 1.5.0** (07-22) — the despawn wand + chest-inventory menu removed for a `/despi
  target` command surface (info · enable/disable/toggle · priority · contraband), per-user
  throttling + void chance + catch-all storage, an optional client-mod protocol (client draws,
  server owns the truth; owner can refuse; nothing the client sends is trusted) with a Fabric
  1.21.x companion mod, automated release screenshots, and multi-platform publishing.
- **Fairy Fox Games 0.25.2/0.25.3** — depth-inside-the-mechanic on the two oldest games still
  missing it: Poise (the Still + Equilibrium, 9th of 13, 07-22) and Symmetry (the Facet + Radiance
  + a secret Infinity stage, 10th of 13, 07-23). Left: Arc + Sluice.
- **Fairy Fox Stories 0.7.1/0.7.2** — the shelf's random override fires for the first time and
  hands the day to *The One-O'Clock Bus* ch.2 (07-22); the next day it doesn't fire and the robbed
  staleness leader *The Wintering House* rotates back for ch.8, opening the book's Secret (07-23).
  Shelf held at 9 books (both GROW).

**Reconciled data components:**

- `_data/projects.yml` — despi → 1.5.0 / Jul 22 (blurb gains per-target controls + optional client
  companion mod + per-user throttling); games → 0.25.3 / Jul 23; stories → 0.7.2 / Jul 23. **The
  site's own meta card was badly stale** (`0.20.7` / Jul 22 / `beta`) because the 07-23 `0.21.0 →
  1.5.0` wave never touched `projects.yml` — brought current to `1.5.1` / Jul 24 / **released**
  (the 1.0.0 first-stable milestone earns the lifecycle move).
- `_data/pulse.yml` — ticker refreshed (site 1.0.0 + hardening wave, both farms' depth, despi
  1.5.0, the first random override; older entries re-aged, oldest dropped); `bars` shifted +2 days.
- `_data/downloads.yml` — **N/A** (the Downloads section was retired in 0.13.1).
- `_projects/` node pages — **N/A** (retired in 0.13.0).
- `hub/registry.yml` — carries no version/lifecycle fields; nothing to reconcile.

**Reconciled the docs library:** the 07-23 standards wave added four `hub/standards/` files that
weren't surfaced in `_docs/`. Added thin summary pages (category `standards`, orders 30-33):
`checklists-are-contracts`, `mandate-ledger`, `ci-secrets`, `docker`. `doc_categories.yml`
unchanged (the `standards` category already exists); About page + header/footer carry no per-project
list to drift.

**Blogged** two day posts (neutral voice): `2026-07-22` (despi wand→commands, Poise depth, the first
random override, the Automated Driver hub onboarding — tags include `site`) and `2026-07-23` (the
site's 1.0.0 first stable release + hardening wave, Symmetry depth, the Wintering House Secret).

Advanced all five `hub/.last-seen.yml` markers to `seen: 2026-07-24`. VERSION `1.5.0 → 1.5.1`.
Build + doc-link gate green.
