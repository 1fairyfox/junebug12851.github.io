# 2026-07-07

_Newest entry on top._

## Maintenance round-up — reconcile to RAP 2.49.6 + Games 0.19.2, onboard the Stories mirror, blog the 6th (0.15.2)

Scheduled maintenance pass over the whole site surface (data files + docs + nodes), not a
blog-only pass. PowerShell + Read/Write/Edit only. Tree clean on `dev` beforehand bar the
untracked `notes/fairyfox-reports/inbound/INBOX-*.md` briefings (left alone — report-review's job).

**References refreshed (all clean fast-forwards, no force-push):**

- **random-ai-prompt** `8f56153 → 5ea0761`, VERSION `2.43.1 → 2.49.6` (all commits 2026-07-06):
  auto-updating (2.44.0), a gallery composer (2.45.0), a repo-root `user/` content overlay (2.46.0),
  a restructure into `engine/` + `targets/` + `targets/desktop → web-shell` (2.47.0), the
  **dynamic-prompts → "blocks"** rename (2.48.0), and an **interactive DPL editor** with hover dials,
  line actions, and a Manage AI-refine/draft-from-description toolbar (2.49.0–2.49.6).
- **fairyfox-games** `5f908ae → 71982e5`, VERSION `0.16.3 → 0.19.2` (all 2026-07-06): an eleventh game
  **Sluice** (colour-sorting; 0.18.0) + white-flash fixes, then the collection **adopted Jekyll** —
  `_games` collection landing, a `/tags/` browse page, `_data/changelog.json`, a Jekyll-build deploy
  (0.19.0), a landing tag filter (0.19.1), a generated README table (0.19.2).
- **pokered-save-editor-2** `1bae326 → acc09b9`, still `0.14.2-alpha` — first change in ~10 days,
  docs-site only: adopted the hub 0.14 chrome + reader, flattened to the reference, rebuilt the
  Overview to exact hub measures, added a themed `/screenshots/` gallery.
- **fairyfox-stories** — new node, **cloned fresh** into `assets/references/` (it turned `reference: true`
  when 0.15.1 registered it). At `0.2.0` (2026-07-06): an AI-managed Jekyll story farm that planted a
  starting shelf of five illustrated books on a pseudo-author system.

**Components reconciled:**

- `_data/projects.yml` — RAP → `2.49.6` / Jul 6; Games → `0.19.2` / Jul 6; PSE2 `updated` → Jul 6
  (docs commits that day, version held at `0.14.2`); site meta → `0.15.2` / Jul 7. Stories already
  accurate; predecessor untouched.
- `_data/pulse.yml` — ticker re-led with the 2026-07-06 highlights, `when` re-dated, `bars` rolled one day.
- `_docs/docs-site.md` — fixed the stale primary-nav enumeration (was missing **Stories**) and added a
  paragraph on the **shared-chrome bundle** (the one reimplement-don't-copy exception, now a vendored
  pull-at-build exact copy).
- `hub/registry.yml`, `_data/doc_categories.yml`, `about.md`, header/footer nav — reviewed, already
  accurate (registry-driven). No node pages / downloads to touch (retired earlier).

**Blogged:** `_posts/2026-07-06-auto-updating-prompts-an-eleventh-game-and-a-shelf-of-stories.md`
(tags `random-ai-prompt, fairyfox-games, fairyfox-stories, pokered-save-editor-2, site, update`) — a
day-centric post across all four sibling threads and the site's own 0.14.5/0.15.0/0.15.1 work. Used
on-domain `/<key>/` project links (the retired `/projects/<key>/` node paths are 404s and were avoided).

**Markers advanced** in `hub/.last-seen.yml`: RAP → `5ea0761`, Games → `71982e5`, PSE2 → `acc09b9`
(seen → 2026-07-07) + a new `fairyfox-stories` block at `803db1f`. New sibling reports left for the
inbound report-review pass (`reports_through` untouched).

**VERSION:** `0.15.1 → 0.15.2` (PATCH). Changelog entry rides in the commit.

**Build:** `JEKYLL_ENV=production bundle exec jekyll build` — _(result recorded on ship)._
