# 2026-07-01 — Session log

_Newest on top._

## Scheduled maintenance round-up — reconcile to RAP 2.35.1 + Games 0.5.1, blog the 30th (0.11.1)

Routine "usual stuff" pass. PowerShell + Read/Write/Edit only (no bash). Tree was clean on `dev`
beforehand — the only untracked files were the `notes/fairyfox-reports/inbound/INBOX-*.md` briefings
from the automated inbound report-review procedure, left untouched (not this pass's job).

**References refreshed.**

- **pokered-save-editor-2** — `fetch` + `merge --ff-only` → "Already up to date" at `1bae326` (still
  `0.14.2-alpha`). No change; nothing to reconcile or blog.
- **random-ai-prompt** — clean fast-forward → `c38373d`, VERSION `2.28.18 → 2.35.1`. All commits dated
  2026-06-30. No force-push (plain ff under the 0.9.6 model).
- **fairyfox-games** — clean fast-forward → `3cd3314`, VERSION `0.4.0 → 0.5.1`. A fifth game (Ricochet)
  plus a depth pass; all commits dated 2026-06-30.

**What the siblings shipped (2026-06-30).**

- **RAP 2.29.0–2.35.1.** The day turned the app outward and honest: self-hosted **Privacy/Terms/Cookies**
  pages + local fonts ending the last IP-to-Google flow (2.30.1); a **real release stage** for the local
  edition (`npm start`; backend factored out of the Vite dev plugin) + file-watch hot reload + atomic
  settings writes (2.32.0); **in-app dialogs** replacing native alert/confirm/prompt (2.34.0); a
  **seedable, deterministic engine** — real threaded PRNG, reproducible-from-seed — + async batch boundary
  (2.35.0); a **README revamp + SEO repositioning** off SD-first and a plain-English **description
  alignment** everywhere (2.33.3 / 2.35.1). Plus a header/provider UX batch (2.30.0–2.32.1),
  natural-language artists/styles + a `{#styles}` block (2.33.0), a unified cookie-free storage overhaul
  (2.29.0), and CI-only security/code-health integrations (Dependabot/CodeQL/Scorecard/Sonar/CodeRabbit +
  Codecov, no bump).
- **Fairy Fox Games 0.4.1–0.5.1.** A mechanically-distinct fifth game **Ricochet** (aim-and-fire one
  bouncing shot; pure `computeShot` core + tests), one on-mechanic depth/feedback addition to **every**
  game (perfects/combos/skims/milestones/chain-bank toasts — all inside existing slots so the UI stays
  clean), landing-page polish, the retired "Fun" surface removed, and first-class-games standing rules.
  110 tests green.

**Reconciled (changed):** `_data/projects.yml` (RAP → 2.35.1 / Jun 30 + blurb aligned to the new
positioning — leads with what it does, DPL demoted, 40+ models; games → 0.5.1 / Jun 30; fixed the **stale
fairyfox.io meta card**, which was two releases behind at 0.9.14 → 0.11.1 / Jul 1), `_data/downloads.yml`
(RAP run command `npm run web` → `npm start`, the proper release stage added in 2.32.0),
`_data/pulse.yml` (ticker re-led with the 06-30 highlights, `when` rolled, bars rolled),
`_projects/random-ai-prompt.md` (seedable/deterministic engine + privacy-by-design stance),
`_projects/fairyfox-games.md` (added Ricochet + a grow-daily note).

**Verified accurate, left untouched:** `_data/downloads.yml` PSE2/PSE1 (unchanged releases),
`hub/registry.yml` (no version fields), `_data/doc_categories.yml`, the `_docs/*` per-project pages
(version-agnostic), `about.md`, header/footer nav (registry-driven).

**Blogged.** `_posts/2026-06-30-legal-pages-a-seedable-engine-and-a-fifth-game.md` (tags
`random-ai-prompt, fairyfox-games, fairyfox-io, site, update`), day-centric — also folds in the site's
own 2026-06-30 work (the 0.10.0 one-seamless-site standards milestone + the 0.11.0 submenu nav), which
had shipped but had no day post yet. Markers advanced in `hub/.last-seen.yml` (RAP → `c38373d`; games →
`3cd3314`; PSE2 unchanged; all `seen` → 2026-07-01). `reports_through` left alone — the new sibling
process reports (RAP ×2, games ×1) are for the inbound report-review pass, not the round-up.

**Notes/version.** July changelog file created + index row added; `VERSION` `0.11.0 → 0.11.1`; this
session log; `status.md` version line + sibling version numbers refreshed. Build-checked before shipping.
