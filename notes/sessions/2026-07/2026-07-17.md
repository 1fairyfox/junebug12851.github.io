# 2026-07-17

## Maintenance round-up pass — reconcile + blog the 16th (0.15.14)

A scheduled maintenance pass. Refreshed all four sibling references, reconciled the data-driven
components against reality, blogged the one complete day found (2026-07-16), and shipped a PATCH.

**References refreshed** (all clean fast-forwards, no force-pushes):

- **pokered-save-editor-2** `9d0fdbd4 → cb8b1d79`, VERSION `0.40.4 → 0.41.8-alpha` — 29 commits, all
  dated **2026-07-16**, a complete day (today is the 17th; the tip is a 07-16 commit). Yesterday's
  parked tip `69e1ef16` is inside this range, as intended. The day the project put its own confident
  claims in front of the cartridge and lost four of them: the **founding Route 22 conflict refuted**
  (`Route22DefaultScript` is an ordered if/else — the 2nd flag is never read; masked, not conflicting),
  **"all flags on crashes" unreproduced** (healthy boot + ~12 map transitions; a slice, not a proof —
  the bulk-set warning is kept but reworded to *unpredictable and unverified*), the **Event Flags UI**
  shipped and immediately found **33 maps with no page at all** (their flags unreachable; 108 → 141
  pages, found by reading the map dropdown, not by a test), and the **remote build red since `0.29.0`**
  — `QImage::flipped` is Qt 6.9+, the kit is 6.11, CI pins 6.8.3, so it compiled locally every time and
  never remotely; the pending Events ship would have merged to `main` and failed to build the
  downloads. Plus a **dev autopilot** that pathfinds and plays the real game (16/16 console-verified),
  map scripts + missables on the Map Storage panel, forged saves at any map, and the PyBoy wedge
  root-caused. Model verified byte-perfect but **14 Pokémon Tower bits mislabelled** → `events.json`
  regenerated from pret, 508 → 2,560 flags.
- **random-ai-prompt** — unchanged at `2f1459f` (`2.60.1`); no commits dated 2026-07-16. Five quiet
  days. It is also the mesh's only standards-bearing sibling, so an unchanged RAP means no hub standard
  could have moved this pass.
- **fairyfox-games** `ebc896c → 450db5d`, VERSION `0.23.0 → 0.23.1` — a pure grow run: **Ink Bloom**
  gets the depth layer (the oldest game without it, carrying the exact plateau the sweep flags). Speed
  is now a smooth asymptote; the **Graze** pays for surviving just outside your own trail's kill radius;
  three chained grazes open **Iridescence** (a doubling window, colour-only, reduced-motion friendly);
  a secret **Eclipse** stage past the visible end, with the printed stage list removed. 4 of 13 games
  now carry the layer.
- **fairyfox-stories** `86c5ffc → 6e78be5`, VERSION `0.4.0 → 0.4.1` — three chapters grown, no plant.
  The **random override fired for the first time in three runs** (slot 1 rolled exactly 18 against a
  ≤18 threshold), landing on *The Wintering House* — so *The Blindfold Act*, the least-complete book,
  was not grown at all. The consequence is recorded plainly and the mechanical draw left to stand.

**Components reconciled:**

- `_data/projects.yml` — PSE2 → `0.41.8` / Jul 16 (blurb gains per-map script progress + all 2,560
  world event flags); games → `0.23.1` / Jul 16; stories → `0.4.1` / Jul 16; RAP left at `2.60.1` /
  Jul 12; the meta fairyfox.io row → `0.15.14` / Jul 17.
- `_data/pulse.yml` — bars shifted one day (appended the **real** 2026-07-16 commit count, 31, counted
  across the four references + this repo; dropped the oldest); ticker rebuilt so the 2026-07-16 work is
  "today" and older entries aged out (the RAP command-injection entry to `4d`).
- `_data/downloads.yml`, `_projects/` node pages — still retired (do not exist); N/A.
- `hub/registry.yml` (no version fields), the `_docs/` library, `_data/doc_categories.yml`, and
  `about.md` — checked, accurate, no change. Verified directly: no sibling's diff since its marker
  touches `hub/`, `standards`, or `fairyfox-reports/`.
- `hub/.last-seen.yml` — markers advanced to `cb8b1d79` (PSE2) / `2f1459f` (unchanged) / `450db5d`
  (games) / `6e78be5` (stories); all `seen` → 2026-07-17, each with a round-up note.

**Blogged:** `_posts/2026-07-16-what-the-console-said-instead.md` — one day-centric post (tags
`pokered-save-editor-2`, `fairyfox-games`, `fairyfox-stories`, `update`). RAP absent (no work that
day). The site itself shipped only this maintenance PATCH, which the round-up post + changelog already
cover, so no separate `[site, update]` post was warranted.

**Version:** `0.15.13 → 0.15.14` (PATCH). Build-checked; shipped dev → main.

_Note: untracked `notes/fairyfox-reports/inbound/INBOX-*.md` files (dated late June–early July) remain
present in the working tree and were left untouched — not this pass's concern, and staging was
file-specific. Second pass running to the same note; worth a deliberate decision from Fairy Fox about
whether they should be tracked, ignored, or removed._

_Snag worth recording: a PowerShell `Set-Content -NoNewline` on `VERSION` collapsed its three comment
lines and the version into one line (`-NoNewline` joins an array with no separator). Caught by reading
the file back; rewritten in full and confirmed against `git diff --stat` as a clean one-line change.
For a single-line edit in a structured file, use the editor tools, not a `Set-Content` round-trip._
