# 2026-07-16

## Maintenance round-up pass — reconcile + blog the 15th (0.15.13)

A scheduled maintenance pass. Refreshed all four sibling references, reconciled the data-driven
components against reality, blogged the one complete day found (2026-07-15), and shipped a PATCH.

**References refreshed** (all clean fast-forwards, no force-pushes):

- **pokered-save-editor-2** `ff38b460 → 69e1ef16`, VERSION `0.39.0 → 0.40.4-alpha` — ~40 commits.
  39 dated 2026-07-15; the tip `69e1ef16` (a `tst_flag_scenarios` wrapper, no version bump) is dated
  **2026-07-16** — a day still in progress — so the marker is parked at the last 07-15 commit
  `9d0fdbd4` and the tip is left for the next pass. The map screen's **edge connections** (Phase 7 —
  the last big object type; neighbour renders full + draggable, view reframed as a Continue-load); a
  **wild-encounter panel** that caught a species/level byte-order model bug before any UI; three more
  state panels (Area, Character, Map Storage) under a keep-derived-values-synced-by-default doctrine;
  and the project's **largest feature yet begun** — naming/classifying all 2,560 world-event flags
  (groundwork only, no UI). Plus a dev-harness upgrade and a CI `qtshadertools` fix.
- **random-ai-prompt** — unchanged at `2f1459f` (`2.60.1`); no commits dated 2026-07-15.
- **fairyfox-games** `3c4855a → ebc896c`, VERSION `0.22.2 → 0.23.0` — **Reprise**, the 14th game on a
  new verb (call-and-response memory); plus Echo Chamber gaining **depth inside its one mechanic**
  (breadth being complete at 13/13, this is the new lever). 3 of 13 now carry the depth layer.
- **fairyfox-stories** `2e953ae → 86c5ffc`, VERSION `0.3.6 → 0.4.0` — grew three chapters, then planted
  **The Blindfold Act**, the shelf's **first mystery** and **first non-magical realm**; also its first
  sequel-lock exercise. An eyeballed grow-pick was honestly walked back to the mechanical blend.

**Components reconciled:**

- `_data/projects.yml` — PSE2 → `0.40.4` / Jul 15 (blurb gains edge connections + wild encounters);
  games → `0.23.0` / Jul 15; stories → `0.4.0` / Jul 15; RAP left at `2.60.1` / Jul 12; the meta
  fairyfox.io row → `0.15.13` / Jul 16.
- `_data/pulse.yml` — bars shifted one day (appended the 2026-07-15 count, dropped the oldest); ticker
  rebuilt so the 2026-07-15 work is "today" and older entries aged out.
- `_data/downloads.yml`, `_projects/` node pages — still retired (do not exist); N/A.
- `hub/registry.yml` (no version fields), the `_docs/` library, `_data/doc_categories.yml`, and
  `about.md` — checked, accurate, no change (no hub standards changed this pass; RAP, the only
  standards-bearer that could have, was unchanged).
- `hub/.last-seen.yml` — markers advanced to `9d0fdbd4` (PSE2, last 07-15 commit) / `2f1459f`
  (unchanged) / `ebc896c` / `86c5ffc`; all `seen` → 2026-07-16, each with a round-up note.

**Blogged:** `_posts/2026-07-15-a-new-verb-a-new-genre-and-the-maps-last-object.md` — one day-centric
post (tags `pokered-save-editor-2`, `fairyfox-games`, `fairyfox-stories`, `update`). RAP absent (no
work that day). The site itself shipped only this maintenance PATCH, which the round-up post +
changelog already cover, so no separate `[site, update]` post was warranted.

**Version:** `0.15.12 → 0.15.13` (PATCH). Build-checked; shipped dev → main.

_Note: untracked `notes/fairyfox-reports/inbound/INBOX-*.md` files (dated late June–early July) remain
present in the working tree and were left untouched — not this pass's concern, and staging was
file-specific._
